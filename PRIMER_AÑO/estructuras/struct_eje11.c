/* Ejercicio 11. Un negocio de paquetería con entregas a domicilio nos ha solicitado un
programa para almacenar datos sobre las calles y las viviendas de su Ciudad. Cada calle
se identificará por dos elementos: Nombre y viviendas que tiene. Además, cada vivienda
estará identificada por el número que ocupa en la calle y la planta. Admitiendo que la
ciudad tiene 5 calles y cada calle tiene cinco viviendas, desarrollar un programa que
permita al usuario:
    a.  Almacenar los datos sobre las calles y las viviendas de la ciudad.
    b. Ver toda la información sobre una calle: número y viviendas que tiene
Nota: Por simplicidad se admitirá que los nombres de las calles no contienen espacios.
----------------------------------------------------------------------------------------
Nota: Para comparar dos string utilizar la función cadena1.compare(cadena2) de la librería
string. Por ejemplo: if(cadena1.compare(cadena2)==0) si es 0 (cero) significa que son iguales
los string.  */

/* Librerias */
#include <stdio.h>
#include <stdlib.h>

/* Directivas */

/* Estructuras */

/* Prototipo de Funciones */

/* Funcion principal main */
int main(int argc, char const *argv[])
{
    /* code */
    return 0;
} // main